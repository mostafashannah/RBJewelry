name: Deploy to Hostinger

on:
  push:
    branches:
      - claude/ecom-shopify-social-ai-agent-M75vk

# Hostinger Node.js handles deployment automatically via GitHub integration.
# This workflow is kept as a no-op placeholder.
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy handled by Hostinger Node.js
        run: echo "✅ Hostinger Node.js auto-deploys on push to this branch"
